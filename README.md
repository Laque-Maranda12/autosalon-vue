# Автосалон - Vue.js сайт

Многостраничный сайт автосалона, созданный на Vue.js 3 с использованием Vue Router.

## Возможности

- 🏠 Главная страница с популярными автомобилями
- 📋 Каталог автомобилей с фильтрацией
- 🚗 Детальная страница автомобиля
- ℹ️ Страница "О нас"
- 📞 Страница контактов с формой обратной связи
- 📱 Адаптивный дизайн для всех устройств

## Технологии

- Vue.js 3
- Vue Router 4
- Vite
- CSS3 (адаптивный дизайн)

## Установка

> Все команды ниже выполняйте в корне проекта (`autosalon-vue`).

1. Установите зависимости:
```bash
npm install
```

## Запуск

Для запуска проекта в режиме разработки:

```bash
npm run dev
```

Проект будет доступен по адресу `http://localhost:5173`

### Быстрый просмотр собранной версии

1. Выполните сборку и предпросмотр с закрепленным портом:

```bash
npm run build && npm run preview:host
```

2. Откройте готовый сайт по ссылке: [http://localhost:4173](http://localhost:4173)

## Сборка

Для создания production сборки:

```bash
npm run build
```

## Структура проекта

```
├── src/
│   ├── components/      # Компоненты (Header, Footer)
│   ├── views/           # Страницы (Home, Catalog, CarDetail, About, Contact)
│   ├── layouts/         # Layout компоненты
│   ├── router/          # Конфигурация роутера
│   ├── App.vue          # Главный компонент
│   ├── main.js          # Точка входа
│   └── style.css        # Глобальные стили
├── index.html
├── package.json
└── vite.config.js
```

## Страницы

- `/` - Главная страница
- `/catalog` - Каталог автомобилей
- `/car/:id` - Детальная страница автомобиля
- `/about` - О нас
- `/contact` - Контакты

