Icon assets for карточки услуг на главной странице (`С пробегом`, `Новые`, `Под ваш бюджет`).

Файлы, которые уже лежат здесь и **подключены в коде** (`src/views/Home.vue`):
- `service-used.svg` — иконка карточки **«С пробегом»** (`/images/icons/service-used.svg`)
- `service-new.svg` — иконка карточки **«Новые»** (`/images/icons/service-new.svg`)
- `service-budget.svg` — иконка карточки **«Под ваш бюджет»** (`/images/icons/service-budget.svg`)

Если нужно заменить иконку, обновите соответствующий файл, сохранив имя. Если добавить новый файл, не забудьте изменить путь в `src/views/Home.vue`.
